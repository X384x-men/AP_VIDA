/* tslint:disable:no-unused-variable */

import { TestBed, waitForAsync } from '@angular/core/testing';
import { HideShowComponentRolDirective } from './hide-show-component-rol.directive';

describe('Directive: HideShowComponent', () => {
  it('should create an instance', () => {
    const directive = new HideShowComponentRolDirective();
    expect(directive).toBeTruthy();
  });
});
