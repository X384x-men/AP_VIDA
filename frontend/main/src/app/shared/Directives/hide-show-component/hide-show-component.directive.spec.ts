/* tslint:disable:no-unused-variable */

import { TestBed, waitForAsync } from '@angular/core/testing';
import { HideShowComponentDirective } from './hide-show-component.directive';

describe('Directive: HideShowComponent', () => {
  it('should create an instance', () => {
    const directive = new HideShowComponentDirective();
    expect(directive).toBeTruthy();
  });
});
